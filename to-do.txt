-create text prompt                                                 CHECK
-fetch image using that prompt                                      CHECK
-automate the previous script(s)                                    IN PROGRESS
-create the shiny app                                               CHECK
    *mvp only showing one image (with max(date))                    CHECK
    *next version with a calendar so you can switch between images  CHECK
-create a database for the images                                   