-create text prompt                                                 CHECK
-fetch image using that prompt                                      CHECK
-automate the previous script(s)                                    
-create the shiny app
    *mvp only showing one image (with max(date))
    *next version with a calendar so you can switch between images
-create a database for the images